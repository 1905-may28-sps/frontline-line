import { PostSearchPipe } from './post-search.pipe';

describe('PostSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PostSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
